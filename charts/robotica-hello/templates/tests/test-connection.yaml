apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "robotica-hello.fullname" . }}-test-connection"
  labels:
    {{- include "robotica-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "robotica-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
