apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "linuxpenguins-website.fullname" . }}-test-connection"
  labels:
    {{- include "linuxpenguins-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "linuxpenguins-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
